services:
  db:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: pcos
    ports:
      - "5432:5432"
    volumes:
      - pcos_pg:/var/lib/postgresql/data

volumes:
  pcos_pg:
